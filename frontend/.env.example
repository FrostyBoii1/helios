# Frontend environment template. Copy to frontend/.env for local non-Docker dev.
# Only VITE_-prefixed vars are exposed to the browser bundle.

# Base URL the browser uses to reach the backend API.
VITE_API_BASE_URL=http://localhost:8000/api/v1
